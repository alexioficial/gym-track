# Public origin of the app (adapter-node uses it for form actions in production).
ORIGIN=http://localhost:5173

# Server-to-server URL of gym-tracker-api. It is never exposed to the browser.
API_URL=http://localhost:8080

# --- Server ---
PORT=3000
